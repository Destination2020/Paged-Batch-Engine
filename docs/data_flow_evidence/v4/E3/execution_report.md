# E3 execution report

E3 is accepted on 2026-09-13. Recovery metadata distinguishes recomputable recipes from preserve-until-released objects and exact continuation from numerical tolerance. Versioned dependencies form a bounded DAG; active, cache and recipe references are separate. In-flight objects and the last necessary non-recomputable replica cannot be evicted. GPU-to-Host demotion reserves Host/staging first, commits after copy/fence/checksum, and only then withdraws the GPU replica.

The real Vision tier probe retained one 262,144-byte feature on GPU, demoted it after verifying the canonical Host copy, restored the same content/representation to GPU, then served a different question from that GPU feature with zero encoder forwards. The cache is explicitly bounded and opt-in; Host remains canonical. The real Language checkpoint run separately demoted five complete prefix pages and physically restored all five with no failure. It restored a 14-token private tail, feature identity/representation, 276 mRoPE position values, RoPE delta, KV progress, RNG counter and outbox cursor before resuming. The first non-exact BF16 token was checked at identical history with logits max/mean errors and top-2 margins, rather than requiring token equality.

Thirty dependency, checkpoint, CUDA migration and failure contracts passed, including Host-full actions, provider/revision failure, last-replica protection and 10,000 lifecycle iterations. Five repeats each of dependency Host/checkpoint, fixed-GPU retain and drop/recompute retain the measured Host/checkpoint overhead and report saved/restored/recomputed work separately.
